# Roblox-Recursive-Function
Some test about the use of a recursive function to copy a table.
To test it just copy to a new Script under ServerScriptService on Roblox Studio.
Then run and checkout on OUTPUT.
